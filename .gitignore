# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
out/
.expo/
.turbo/
*.tsbuildinfo

# Env files (NEVER commit secrets — Daraja keys, DB URLs)
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json

# Prisma
**/prisma/migrations/dev.db*

# Local portable Postgres cluster (dev only — binaries + data dir)
.local-postgres/

# Coverage
coverage/

# Source media (raw phone photos and video, ~4GB). Working library only —
# 14 of the videos exceed GitHub's 100MB per-file limit. Web-ready derivatives
# live in apps/web/public/photos/ and are committed.
/Media/
