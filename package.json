{
  "name": "onyxhawk",
  "version": "0.0.0",
  "private": true,
  "description": "OnyxHawk — premium cleaning service platform",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "rimraf": "^6.0.1",
    "@types/node": "^22.10.5"
  }
}
