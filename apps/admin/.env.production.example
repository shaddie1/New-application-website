# Production env for the admin portal.
# Set this in the Cloudflare Pages project (Settings → Environment variables).

# Base URL of the deployed API. Must be HTTPS and CORS-allowed by the API.
NEXT_PUBLIC_API_URL=https://api.onyxhawkcleaningservice.com
